<% provide(:title, full_name(@user)) %>
<div class="row">
	<aside class="large-2 columns">
		<section class="user-area">
			<%= gravatar(@user) %>
			<h4 class="user-area-title"><%= full_name(@user) %></h4>
		</section>
	</aside>	
</div>