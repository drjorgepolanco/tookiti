- if @user.errors.any?
  .div{class: 'alert-box warning', 'data-alert' => ''}
    %h4 the form contains #{pluralize(@user.errors.count, 'error')}
    %ul
      - @user.errors.full_messages.each do |msg|
        %li= msg.downcase